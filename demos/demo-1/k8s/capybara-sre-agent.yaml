apiVersion: apps/v1
kind: Deployment
metadata:
  name: capybara-sre-agent
  labels:
    app: capybara-sre-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: capybara-sre-agent
  template:
    metadata:
      labels:
        app: capybara-sre-agent
    spec:
      containers:
        - name: capybara-sre-agent
          image: capybara-sre-agent:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8088
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-secret
                  key: api-key
            - name: CAPYBARA_MCP_URL
              value: "http://capybara-db-mcp:8086/mcp/sse"
            # One variable serves all three datasources — app, kangaroo and admin —
            # because each appends its own ApplicationName to this base URL.
            - name: CAPYBARA_DB_URL
              value: "jdbc:postgresql://capybara-db:5432/capybara"
            # mcp (default) or local: which instrumentation sees the tool call.
            - name: CAPYBARA_TOOLS
              value: "mcp"
            - name: OTEL_JAVAAGENT_ENABLED
              value: "false"
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: grpc
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector-opentelemetry-collector.default.svc.cluster.local:4317"
          livenessProbe:
            httpGet:
              path: /q/health
              port: 8088
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /q/health
              port: 8088
            initialDelaySeconds: 15
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: capybara-sre-agent
  labels:
    app: capybara-sre-agent
spec:
  selector:
    app: capybara-sre-agent
  ports:
    - port: 8088
      targetPort: 8088
      protocol: TCP
  type: ClusterIP
